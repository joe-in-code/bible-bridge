# bible-bridge
Light Script is a guided biblical‑retelling environment that allows users to creatively reinterpret biblical stories while preserving theological integrity. The app is lightweight: the frontend handles UI state and selectors, while the backend formats metadata into structured prompts and sends them to an AI model for retelling generation.
